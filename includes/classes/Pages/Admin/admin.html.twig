{% extends "@grid/page.html.twig" %}
{% set css = "admin" %}

{% block maincontent %}

    <div class="container">
        <div class="row">
            <div class="col">
                <h1>{{ site.getString(38) }}</h1>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <h2>{{ site.getString(37) }}:</h2>
                <ul>
                    <li><a href="#">{{ site.getString(33) }}</a></li>
                    <li><a href="#">{{ site.getString(34) }}</a></li>
                </ul>
            </div>
        </div>
    </div>

    {{ site.renderPageTiles }}

{% endblock %}